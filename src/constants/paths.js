import path from 'node:path';

export const EMAIL_TEMPLATES_DIR = path.join(process.cwd(), 'templates');
export const TEMP_DIR = path.join(process.cwd(), 'temp');
export const UPLOADS_DIR = path.join(process.cwd(), 'uploads');
